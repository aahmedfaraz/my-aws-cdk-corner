// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as Example03SendEmailsUsingSesAndLambda from '../lib/example03_send_emails_using_ses_and_lambda-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/example03_send_emails_using_ses_and_lambda-stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new Example03SendEmailsUsingSesAndLambda.Example03SendEmailsUsingSesAndLambdaStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
